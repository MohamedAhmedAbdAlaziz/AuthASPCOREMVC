@model Employee


@{
    ViewData["Title"] = "Home Page";
    var Photopa= "~/images/"+(Model.PHotoPath?? "mosa.jpg");
}


<div class="card-deck"> 
 
    <div class="card-header">
        <h3>@Model.Name</h3>

    </div>
<img class="card-img-top " src="@Photopa"   asp-append-version="true"style="max-width: 200px;">
<div class="card-footer text-center">
    <a asp-controller="home" asp-action="index" class="btn btn-primary">Back</a>
    <a  class="btn btn-primary" asp-controller="home" asp-action="ُEdit" asp-route-id="@Model.Id">Edit</a>
    <a href='#' class="btn btn-primary">Delete</a>
</div>
  
    
</div>